// starting from
// `use c`
// with auto insert, select `crate::` then type `m` without selecting. Then select `main` without accepting will turn this
use crate::main;
// into
// use mainm

fn main() {}
